# LitOrganizer Dependencies
# Core requirements for PDF processing and metadata extraction

# Web UI
Flask==3.0.3           # Web framework for the local web interface
flask-socketio==5.3.7  # WebSocket support for real-time progress updates

# PDF Processing
PyMuPDF==1.24.14       # Fast PDF text extraction (fitz)
pdfplumber==0.11.4     # Advanced PDF text extraction with layout awareness

# OCR Support (Optional - requires Tesseract OCR installed on system)
pytesseract==0.3.13    # Optical Character Recognition interface to Tesseract
pdf2image==1.17.0      # Convert PDF pages to images (for OCR)
Pillow==10.4.0         # Python Imaging Library for image processing

# Document Generation
python-docx==1.1.2     # Creating Word documents for search results
pandas==2.2.3          # Data handling and Excel file generation
openpyxl==3.1.5        # Excel file writer (used by pandas)

# HTTP and API
requests==2.32.3       # HTTP requests for DOI/Gemini AI metadata retrieval
