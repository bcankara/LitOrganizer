services:
  litorganizer:
    image: bcankara/litorganizer:v2
    ports:
      - "5000:5000"
    volumes:
      - ./pdfs:/app/pdf
      - ./processed:/app/processed
      - ./logs:/app/logs
      - ./config:/app/config
    environment:
      - LITORGANIZER_HOST=0.0.0.0
    restart: unless-stopped
